[package]
name = "expanse_tracker"
version = "0.1.0"
edition = "2021"

# See more keys and their definitions at https://doc.rust-lang.org/cargo/reference/manifest.html

[dependencies]
chrono = "0.4"
rusqlite = "0.27"
inquire = { version = "0.2", features = ["date"]}
